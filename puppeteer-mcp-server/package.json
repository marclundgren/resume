{
  "name": "resume-pdf-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for exporting resume as high-quality PDF using Puppeteer",
  "type": "module",
  "bin": {
    "resume-pdf-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "puppeteer": "^23.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
